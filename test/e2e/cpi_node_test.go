package e2e

import (
	. "github.com/onsi/ginkgo"
)

var _ = Describe("Recreating VMs", func() {
	It("should result in new nodes", func() {
		// TODO(fhan)
	})
})

var _ = Describe("Restarting VMs", func() {
	It("should pertain the original node", func() {
		// TODO(fhan)
	})
})
